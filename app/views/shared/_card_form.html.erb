<%= simple_form_for @card, validate: true do |f| %>
  <%= f.input :place_id %>

  <div class="d-flex justify-content-between">
    <%= f.submit class: 'btn btn-primary mb-2' %>
    <%= link_to 'Cancel', new_card_path, class: 'btn btn-outline-secondary mb-2' %>
  </div>
<% end %>
